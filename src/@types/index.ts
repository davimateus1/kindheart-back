export enum GenderEnum {
  MALE,
  FEMALE,
  NOT_INFORM,
}

export enum RoleEnum {
  ADMIN,
  VOLUNTARY,
  ELDERLY,
}

export enum StatusEnum {
  FREE,
  ALOCATED,
  STARTED,
  FINISHED,
  CANCELED,
}
